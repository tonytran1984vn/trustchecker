# ═══════════════════════════════════════════════════════════════════
# TrustChecker v9.4 — Environment Variables
# Copy to .env and fill in values
# ═══════════════════════════════════════════════════════════════════

# ─── Server ────────────────────────────────────────────────────────
PORT=4000
NODE_ENV=development          # development | production | test

# ─── Authentication (REQUIRED in production) ──────────────────────
# Generate: openssl rand -hex 64
JWT_SECRET=trustchecker-secret-key-DEV-ONLY

# ─── Encryption (REQUIRED in production) ──────────────────────────
# Generate: openssl rand -hex 32
ENCRYPTION_KEY=trustchecker-encryption-key-DEV-ONLY

# ─── Database ─────────────────────────────────────────────────────
# PostgreSQL (recommended for production):
# DATABASE_URL=postgresql://user:password@localhost:5432/trustchecker
#
# SQLite (default for development — no config needed, uses ./data/trustchecker.db):
# Leave DATABASE_URL unset to use SQLite

# ─── Redis (optional — enables caching, queues, pub/sub) ─────────
# REDIS_URL=redis://localhost:6379

# ─── Read Replica (optional — enables CQRS read scaling) ─────────
# DATABASE_READ_URL=postgresql://user:password@read-replica:5432/trustchecker

# ─── CORS (REQUIRED in production — no wildcards!) ────────────────
# Comma-separated list of allowed origins
CORS_ORIGINS=*

# ─── License Keys (optional — auto-generated in dev) ─────────────
# LICENSE_PUBLIC_KEY_PATH=/path/to/license-public.pem
# LICENSE_PRIVATE_KEY_PATH=/path/to/license-private.pem

# ─── Webhook Secrets ──────────────────────────────────────────────
# WEBHOOK_SECRET=your-stripe-webhook-secret

# ─── AI/ML Engine URLs (optional) ────────────────────────────────
# AI_SIMULATION_URL=http://localhost:5001
# AI_DETECTION_URL=http://localhost:5002
# AI_ANALYTICS_URL=http://localhost:5003

# ─── Observability (optional — OpenTelemetry) ────────────────────
# OTEL_SERVICE_NAME=trustchecker-api
# OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4318
# OTEL_SAMPLE_RATE=1.0
# OTEL_BATCH_FLUSH_MS=5000
# OTEL_BATCH_MAX_SIZE=512
# APP_VERSION=9.4.0

# ─── Logging ──────────────────────────────────────────────────────
# LOG_LEVEL=info              # debug | info | warn | error
# SERVICE_NAME=trustchecker
